import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.pexels.com', 'www.pexels.com','aceternity.com','cdn.sanity.io'], // Add the domains you are using
  },
};

export default nextConfig;
